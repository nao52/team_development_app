<div>
  <h3><%= article.title %></h3>
  <p><%= article.body %></p>
  <p>作成者:<%= "#{article.user.last_name}#{article.user.first_name}" %></p>
  <p><%= article.created_at %></p>
  <p>
    <%= link_to "記事の編集", edit_article_path(article) %> |
    <%= link_to "記事の削除", article, data: { "turbo-method": :delete, turbo_confirm: "#{article.title}を削除しますか？" } %>
  </p>
</div>

<hr>